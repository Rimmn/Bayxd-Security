name: 🔒 Protect Bot Files

on:
  push:
    branches: [main]
    # HAPUS "paths:" atau biarkan kosong untuk semua file
    # paths: []  ← HAPUS INI atau COMMENT

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: 🔐 Simple Check
        run: |
          echo "User: ${{ github.actor }}"
          
          # HANYA RIMMN YANG BOLEH
          if [ "${{ github.actor }}" != "Rimmn" ]; then
            echo "❌ ERROR: Only Rimmn can modify files!"
            exit 1
          fi
          
          echo "✅ OK: Rimmn is authorized"
